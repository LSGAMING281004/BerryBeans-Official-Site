import React from 'react';

const AnimatedLogo = ({ className }) => {
    return (
        <img
            src="/logo.png"
            alt="BerryBeans Logo"
            className={className}
        />
    );
};

export default AnimatedLogo;
